#include "main.h"
/**
  *_abs - this function return the abslut Number of input
  *
  *Return: the abslut number
  *@n: this number input
  */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else if (n < 0)
		return (-n);

	return (0);
}

